{
  "questions": [
    {
      "id": "q1",
      "question": "Transport for the token POST: fetch (per §4 recommendation) or ctx.httpRequest.send + bodyPath?",
      "type": "single_select",
      "options": ["fetch (global Node)", "ctx.httpRequest.send + bodyPath", "spike both, decide later"],
      "recommended_answer": "fetch (global Node)",
      "confidence": "low"
    },
    {
      "id": "q2",
      "question": "Assertion JWT lifetime (exp = iat + 3600, §2) — keep fixed at 1h, or make it shorter/configurable?",
      "type": "single_select",
      "options": ["fixed 3600s", "shorter fixed (e.g. 300s)", "configurable arg"],
      "recommended_answer": "fixed 3600s",
      "confidence": "high"
    },
    {
      "id": "q3",
      "question": "Concurrent cache-miss renders for the same key — de-duplicate in-flight mints, or allow redundant parallel POSTs?",
      "type": "single_select",
      "options": ["allow redundant (document)", "de-dup via in-flight promise map", "defer, not a concern"],
      "recommended_answer": "allow redundant (document)",
      "confidence": "low"
    },
    {
      "id": "q4",
      "question": "What does the failure toast surface from a NetSuite non-2xx body (§1)?",
      "type": "single_select",
      "options": ["error + error_description only", "full response body", "generic static message"],
      "recommended_answer": "error + error_description only",
      "confidence": "low"
    },
    {
      "id": "q5",
      "question": "Does `yaak plugin build` bundle the new `jose` runtime dependency, or is extra config needed?",
      "type": "single_select",
      "options": ["bundled by build (verify)", "needs explicit bundling config", "vendor jose into source"],
      "recommended_answer": "bundled by build (verify)",
      "confidence": "low"
    },
    {
      "id": "q6",
      "question": "Cache key construction (§2 vs §3 wording): plain field join or hashed?",
      "type": "single_select",
      "options": ["plain delimited join", "hashed (sha256)"],
      "recommended_answer": "plain delimited join",
      "confidence": "high"
    },
    {
      "id": "q7",
      "question": "scope arg: keep single text field defaulting to rest_webservices, or support multiple space-delimited scopes explicitly?",
      "type": "single_select",
      "options": ["single text, space-delimited allowed", "multi-select of known scopes", "single fixed value, no arg"],
      "recommended_answer": "single text, space-delimited allowed",
      "confidence": "low"
    }
  ]
}
